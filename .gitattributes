# --- Text files ---
# Normalize line endings to LF for cross-platform consistency
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.json text eol=lf
*.md   text eol=lf

# --- Images ---
# Treat images as binary (no diffs/merges)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.svg text eol=lf   # SVG is XML-based, so keep as text

# --- Fonts ---
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary

# --- Media (animations, videos, audio) ---
# Use Git LFS for large files to avoid bloating repo
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.webm filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text

# --- Archives ---
*.zip binary
*.tar binary
*.gz  binary

# --- Special handling ---
# Ensure shell scripts use LF endings
*.sh text eol=lf
